project:
  type: website
  output-dir: docs

format:
  revealjs:
    incremental: true
    theme: dracula
    css: styles.css

# Automatically create a .nojekyll file in the docs folder
execute:
  freeze: true
  require-files:
    - docs/.nojekyll
